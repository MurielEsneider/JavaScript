function ejercicio30(){
    let numeroIngresado = parseInt(prompt("Ingresa un número para comenzar la cuenta regresiva:"));

    while (numeroIngresado > 0) {
    alert(numeroIngresado);
    numeroIngresado = numeroIngresado - 1;
    }

}